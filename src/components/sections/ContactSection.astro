---
import portfolioData from '../../data/portfolio.json';

const { contact } = portfolioData;
---

<section class="px-6 py-24 sm:py-28" id="contact">
  <div class="relative z-10 mx-auto grid max-w-6xl gap-10 border-y border-[var(--color-border)] py-14 lg:grid-cols-2 lg:grid-rows-[auto_auto] lg:gap-x-24 lg:gap-y-8 lg:py-20">
    <div>
      <p class="text-xs font-semibold uppercase tracking-[0.22em] text-[var(--color-accent)]">{contact.eyebrow}</p>
      <h2 class="mt-5 max-w-lg text-3xl font-bold leading-tight tracking-tight text-[var(--color-text-primary)] sm:text-4xl">
        함께 나누고 싶은<br />이야기가 있나요?
      </h2>
    </div>

    <div class="flex items-center lg:row-span-2">
      <p class="max-w-xl text-base font-normal leading-relaxed text-[var(--color-text-secondary)] sm:text-lg sm:leading-relaxed">
        {contact.description}
      </p>
    </div>

    <div class="lg:col-start-1 lg:row-start-2">
      <a href="/contact" class="inline-flex rounded-lg border border-[var(--color-accent)] bg-[var(--color-accent)] px-6 py-3 text-sm font-normal text-white transition-colors duration-200 hover:bg-white hover:text-[var(--color-accent)]">
        Contact Me!
      </a>
    </div>
  </div>
</section>
