# 포트폴리오 UI 디자인 시스템

이 문서는 포트폴리오에서 새 버튼, 링크, 태그, 카드형 인터페이스를 만들 때 따라야 하는 기준이다. 새 UI를 추가할 때 기존 화면에서 스타일을 복사하지 말고 이 문서와 공통 컴포넌트를 우선 사용한다.

## 1. 핵심 색상

| 역할 | 값 | 사용처 |
| --- | --- | --- |
| Brand navy | `#0d1b36` | 본문, 버튼 텍스트, 유리의 깊은 음영 |
| Dusty rose surface | `#b85f73` | 주요 CTA 표면, 큰 강조 그래픽 |
| Dusty rose text | `#974b60` | 작은 라벨, 링크, 본문 안의 강조 |
| Dusty rose hover | `#7d3a4f` | 링크 호버 및 강한 텍스트 강조 |
| Dusty rose subtle | `rgba(184, 95, 115, 0.12)` | 배지와 약한 활성 배경 |

주황색은 사용하지 않는다. 새로운 강조색이 필요하면 임의의 색을 추가하지 말고 위 토큰의 투명도를 조절한다.

## 2. 버튼 형태

- 모든 텍스트 CTA는 완전한 캡슐형이다: CSS `border-radius: 999px`, `rounded-full`, `LiquiGlass radius={999}`.
- 아이콘 전용 이동 버튼은 정원형이다. 너비와 높이를 같게 하고 `rounded-full`을 적용한다.
- 카드와 큰 패널은 캡슐형으로 만들지 않는다. 네비게이션과 같은 부드러운 22~36px 모서리를 사용한다.
- 버튼 높이, 좌우 패딩, 아이콘 간격은 공통 `LiquidActionLink`가 결정한다.

## 3. 버튼 우선순위

### Primary glass

- 용도: 페이지의 대표 행동, `Contact Me`, 대표 GitHub 저장소, 서비스 바로가기.
- 표현: 더스티 로즈 틴트와 네이비 깊은 음영을 가진 굴절 유리.
- 텍스트와 아이콘: 브랜드 네이비.
- 구현: `LiquidActionLink variant="accent"` 또는 `LiquidContactButton`.

### Clear glass

- 용도: 보조 행동, 두 번째 저장소, 이전/다음 단계, 부가 링크.
- 표현: 색을 채우지 않은 투명 굴절 유리와 밝은 테두리.
- 텍스트와 아이콘: 브랜드 네이비.
- 구현: `LiquidActionLink variant="clear"`.

한 그룹에 여러 버튼이 있으면 Primary는 하나만 둔다. 나머지는 Clear glass를 사용한다.

## 4. 기술 스택 칩

- 기술 스택은 행동 버튼이 아니라 정보 태그다.
- 배경색을 채우지 않고 `background: transparent`를 사용한다.
- 얇은 흰색 반투명 테두리, 약한 내부 하이라이트, 배경 블러만 허용한다.
- 형태는 작은 캡슐형이며 호버 시 색이 크게 변하거나 떠오르지 않는다.
- 예: `RAG`, `FastAPI`, `Supabase`.

## 5. 아이콘 규칙

- `+`: 새로운 주요 영역으로 진입하는 히어로 CTA에만 사용한다.
- `→`: 같은 사이트 안의 다음 단계로 이동할 때 사용한다.
- `↗` 형태: GitHub와 외부 저장소처럼 새 탭으로 이동할 때 사용한다.
- 네비게이션의 `Contact Me`에는 아이콘을 넣지 않는다.
- 메인 페이지 마지막 `Contact Me`에는 오른쪽 화살표를 사용한다.

## 6. 공통 구현

```tsx
import { LiquidActionLink, LiquidContactButton } from '../components/ui/LiquidHeroGlass';

<LiquidActionLink
  href="https://github.com/example/repository"
  label="GitHub Repository"
  variant="accent"
  icon="external"
  external
/>

<LiquidActionLink
  href="https://github.com/example/secondary"
  label="Related Repository"
  variant="clear"
  icon="external"
  external
/>

<LiquidContactButton icon="none" />
```

페이지에서 더스티 로즈 버튼 CSS를 새로 작성하지 않는다. 크기나 링크가 달라도 공통 컴포넌트의 props로 해결한다.

## 7. 접근성과 동작

- 링크 목적을 텍스트만으로 이해할 수 있어야 한다.
- 외부 링크에는 `target="_blank"`와 `rel="noopener noreferrer"`를 함께 적용한다.
- 키보드 포커스 링을 제거하지 않는다.
- `prefers-reduced-motion`에서는 확대 및 이동 애니메이션을 비활성화한다.
- 투명 유리 위의 텍스트는 배경과 충분한 대비를 유지하고 광택 레이어보다 위에 배치한다.

## 8. 전역 배경 애니메이션

- 유리 굴절이 보이도록 모든 페이지 뒤에는 `AmbientGlassBackground`를 사용한다.
- 색상은 브랜드 네이비, 더스티 로즈, 밝은 블루 화이트만 사용한다.
- 도형은 화면보다 큰 리본과 유기적인 덩어리로 구성하고 서로 다른 18~29초 주기로 계속 움직인다.
- 배경 애니메이션은 콘텐츠보다 시각적 우선순위가 낮아야 하며 클릭과 스크롤을 방해하지 않는다.
- 새 페이지에서 별도의 배경 장식을 추가하지 않는다. 전역 배경을 그대로 사용한다.
- `prefers-reduced-motion: reduce` 환경에서는 모든 배경 애니메이션을 중지한다.

## 9. 텍스트 가독성 보호층

- 텍스트를 움직이는 배경 위에 직접 올리지 않는다.
- 홈의 각 섹션은 밝은 방사형 보호 그라데이션을 사용하고, 히어로는 텍스트 주변의 보호 강도를 더 높인다.
- Product와 Study 같은 상세 페이지는 전체 콘텐츠 아래에 약 48% 밝기의 보호층을 둔다.
- 보호층은 배경을 완전히 가리지 않아야 하며 유리 표면 주변에서는 도형 경계가 계속 보여야 한다.
- 일반 텍스트는 `#0d1b36` 또는 `#42516a`, 흐린 텍스트는 최소 `#5b6880`을 사용한다.
- `#b85f73`은 버튼 표면과 큰 강조 글자에 사용하고, 작은 강조 글자는 대비가 높은 `#974b60`을 사용한다.
